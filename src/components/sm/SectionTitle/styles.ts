import styled from "styled-components";

export const StyledDiv = styled.div `
    margin-bottom: 40px;
    max-width: 374px;

    span {
        font-size: 14px;
        font-weight: bold;
        color: var(--orange);
        text-transform: uppercase;
        margin-bottom: 8px;
    }

    h1 {
        font-size: 36px;
    }
`;